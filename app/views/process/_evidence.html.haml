- last_cg = last_name = nil
%table
  %thead
    %th ชื่อ
    %th วัน/เวลา ยื่นเอการ
    %th
  %tbody
    - Evidence.all.each do |ev|
      %tr
        %td= ev.name
        - att = @application.attachments.where(evidence: ev)
        %td
          - if att.count > 0
            = l(ev.created_at,format: :long)
          - else
            %span.red-text - ยังไม่ได้แนบ -
        %td
          = link_to t(:download), rails_blob_path(att.data, disposition: "attachment") if att.count > 0


