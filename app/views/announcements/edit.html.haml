.row
  .col.s6.offset-s3
    .card
      .card-content.deep-purple
        .card-title
          %h1.white-text= t(:edit) + t(:announcement, scope: 'activerecord.models')
      .card-content
        = render partial: 'form', locals: {back_link: announcements_path}
